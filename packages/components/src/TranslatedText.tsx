// packages/components/src/TranslatedText.tsx
import React from 'react';

export function useTranslatedText(text: string): { text: string } {
  // Just return the input for now; replace with real i18n logic if needed
  return { text };
}
