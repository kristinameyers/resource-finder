Instructions
Remove the vertical space/gap between the section containing "Search Category" and "Search Keyword" buttons (the nav bar), and the icon grid section beneath.

Icon grid section ("Browse all Categories" and category icons) should extend fully edge-to-edge (flush with left and right edges of the screen), as shown in your mockup.

The icon grid section background, padding, and border radius should be updated so it visually matches the mockup—with no margin or gap separating it from the button/nav bar above.

Example CSS for Immediate Results
1. Remove Space Between Button Nav and Icon Grid
css
/* Navigation bar CSS -- unchanged, except adjust bottom margin */
.menu-nav-bar {
  background: #62a0c6;
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 32px;
  padding: 32px 0 0 0; /* Remove bottom padding, or set to 0 */
  margin-bottom: 0;     /* No extra spacing below buttons */
}
2. Icon Grid Section — Edge-to-Edge Layout
css
.icon-grid-section {
  width: 100vw;           /* Make grid take full viewport width */
  margin-left: 50%;
  transform: translateX(-50%);  /* Center section if needed, removes browser margin issues */
  background: #0458a3;    /* Use your rich blue background */
  border-radius: 0;       /* Remove rounded corners for full-bleed effect */
  padding: 32px 0 32px 0; /* Vertical padding, no horizontal padding */
  box-sizing: border-box; /* Prevent box overflow */
}

/* If inside a container, ensure parent width is also 100vw or unset container padding/margin */

.page-container {
  width: 100vw;
  margin: 0;
  padding: 0;
  background: #fff;
}
3. Remove Default Body/Page Container Padding
css
body, .page-container {
  margin: 0;
  padding: 0;
  background: #fff;
  box-sizing: border-box;
}
4. Example HTML Structure & Notes
xml
<div class="menu-nav-bar">
  <button class="menu-btn-category">Search Category</button>
  <button class="menu-btn-keyword">Search Keyword</button>
</div>
<div class="icon-grid-section">
  <div class="icon-grid-header">Browse all Categories</div>
  <div class="icon-grid">
    <!-- category icons go here -->
  </div>
</div>
The .icon-grid-section should have no margin-top.

The icon grid must fill the entire width without white space on the left/right.

Button bar is immediately above, with no visible spacing between the sections.

Summary of Directions
Set margin-bottom: 0 and remove or reduce any bottom padding on the button nav bar.

Apply width: 100vw, margin: 0, border-radius: 0, and proper background color to the icon grid section.

Eliminate body/container horizontal padding to ensure full-bleed effect.

Check that the icon grid is nested directly after the button section, with zero intervening spacing.